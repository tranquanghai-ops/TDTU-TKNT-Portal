#!/usr/bin/env node
console.log('Local production deployment is disabled.');
console.log('Run npm run build for a local verified build, then merge a reviewed Portal PR into main.');
console.log('Only the Portal Deploy Production GitHub Actions workflow may deploy tknt-tdtu Hosting live.');
